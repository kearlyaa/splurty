<!DOCTYPE html>
<html>
  <head>
    <title>Splurty</title>
    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
    <link rel="icon" type="image/png" href="<%= asset_path( 'favicon.png' ) %>" />
  </head>
  <body>

    <header>
      
      <div class="col-xs-12 col-sm-2">
        <%= link_to root_path, class: 'logo' do %>
          <div class="circle">
            <br />
            You know
            <br />
            you need
            <br />
            to code
          </div>
        <% end %>  
      </div>

      <div class="col-xs-12 col-sm-4 slogan hidden-xs">
        Spouting words of wisdom and nonsense
      </div>

      <div class="col-xs-8 col-xs-offset-1 col-sm-offset-0 col-sm-5 links">
        <%= link_to 'Contribute', '#', data: {toggle: 'modal', target: '#myModal'} %>
        <%= link_to 'About', about_path %>
      </div>

    </header>

    <!-- Button trigger modal -->
 

  <!-- Modal -->
  <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
          <h4 class="modal-title" id="myModalLabel">Add Your Quote</h4>
        </div>
        <%= simple_form_for Quote.new do |f| %>
          <div class="modal-body">
            <h1>Enter Your Quote Details</h1>
            
              <%= f.input :saying, label: "What is the saying you want to add?", placeholder: "Work it harder, make it better.  Do it faster, makes us stronger.  More than ever, hour after.  Our work is never over" %>
              <%= f.input :author, label: "Name of the quote author", placeholder: "Daft Punk" %>
                        
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            <%= f.submit 'Add Quote', class: 'btn btn-danger' %>
          </div>
        <% end %>
        
      </div>
    </div>
  </div>

    <br class="clear" />
    
    <hr />

    <div class="container">
      
      <% if flash[:error].present? %>
        <div class="col-xs-10 col-xs-offset-1 alert alert-danger">
          <%= flash[:error].html_safe %>
        </div>
      <% end %>

      <%= yield %>
    </div>

    <div class="footer">
      <div class="container">
        <div class="col-xs-12 col-sm-6">
           <%= link_to "This isn't enough, I need more", root_path, class: 'next-quote' %>
        </div>
        <div class="col-xs-12 col-sm-6 footer-blurb">
          Want to build your own Splurty Web App - <a href="http://thefirehoseproject.com">Learn how to code here</a>
        </div>
      </div>

  </body>

</html>
